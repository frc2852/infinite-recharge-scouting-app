// to define a var:

const Balls = 3;

// do NOT use Balls ++
// vars can only be edited using:

let Balls = 3;
balls ++;

// to create a function:

function addBall(number){
      return number + 1;
}

// if statements:

if (balls<3){
      balls = addBall(balls);
}
